@page "{id:int}"
<!-- Esto indica que la pagina acepta un parametro llamado id -->
@model RetoOxxoWeb.Pages.Perfil.EditarPerfilModel
@{
    ViewData["Title"] = "Editar Perfil";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="~/css/EditarPerfil.css" asp-append-version="true" />
    <title>Document</title>
</head>
<body>
    
    <h2>Editar Perfil</h2>
    <form method="post" class="formulario-perfil">
        <!-- Formulario que enviará los datos mediante método POST-->

        <input type="hidden" asp-for="Usuario.id_usuario"/>
        <!--Campo oculto para enviar el ID del usuario, necesario para hacer el UPDATE correcto-->

        <div asp-validation-summary="All" class="text-danger"></div>
        <!-- Muestra los errores de validación del modelo, si los hay-->

        <!-- Campos del modelo usuario -->
        <div class="group-input">
            <label>Nombre de usuario:</label>
            <input asp-for="Usuario.nom_usuario" class="form-control" />
        </div>

        <div class="group-input">
            <label>Nombre:</label>
            <input asp-for="Usuario.nombre" class="form-control" />
        </div>

        <div class="group-input">
            <label>Apellido Paterno:</label>
            <input asp-for="Usuario.apellidop" class="form-control" />
        </div>

        <div class="group-input">
            <label>Apellido Materno:</label>
            <input asp-for="Usuario.apellidom" class="form-control" />
        </div>

        <div class="group-input">
            <label>Teléfono:</label>
            <input asp-for="Usuario.telefono" class="form-control" />
        </div>

        <div class="group-input">
            <label>Fotografía (URL):</label>
            <input asp-for="Usuario.fotografia" class="form-control" />
        </div>

        <div class="group-input">
            <label>Tipo de Empleado:</label>
            <input asp-for="Usuario.tipo_empleado" class="form-control" />
        </div>

        <div class="group-input">
            <label>Contraseña:</label>
            <input asp-for="Usuario.contraseña" class="form-control" type="password" />
        </div>

        <div class="group-input">
            <label>Calle:</label>
            <input asp-for="Usuario.calle" class="form-control" />
        </div>

        <div class="group-input">
            <label>Estado:</label>
            <input asp-for="Usuario.estado" class="form-control" />
        </div>

        <div class="group-input">
            <label>Ciudad:</label>
            <input asp-for="Usuario.ciudad" class="form-control" />
        </div>

        <div class="group-input">
            <label>Código Postal:</label>
            <input asp-for="Usuario.cp" class="form-control" />
        </div>

        <button type="submit" class="btn btn-primary mt-3">Guardar Cambios</button>
        <!-- Botón para enviar el formulario. Usa clases de estilo para apariencia Bootstrap o personalizada -->
    </form>

</body>
</html>
